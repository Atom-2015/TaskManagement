import React from "react";

const StatusButton = () => {
  return (
    <button
      className={` text-center text-xs font-[14px] px-2 py-1 rounded `}
    //   onClick={() => openModal(task)}
    >
    Update Task
    </button>
  );
};

export default StatusButton;
